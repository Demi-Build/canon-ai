# Byte-compiled / optimized / DLL files
__pycache__/
*.py[cod]
*$py.class

# C extensions
*.so

# Distribution / packaging
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
*.egg

# PyInstaller
*.manifest
*.spec

# Installer logs
pip-log.txt
pip-delete-this-directory.txt

# Unit test / coverage reports
htmlcov/
.tox/
.nox/
.coverage
.coverage.*
.cache
nosetests.xml
coverage.xml
*.cover
*.py,cover
.hypothesis/
.pytest_cache/

# Environments
.env
.env.*
!.env.example
.venv/
env/
venv/
ENV/

# IDE
.vscode/
.idea/
*.swp
*.swo
*~
.cursor/

# Ruff
.ruff_cache/

# mypy / pyright
.mypy_cache/
.pyright/

# Jupyter
.ipynb_checkpoints/

ROADMAP_HANDOFF.md
.claude/

# Generated platformer output trees — never source. Prefer --output-dir OUTSIDE
# the repo; plat_slice_output is the runner default that lands here otherwise
# (a dropped `\` on --output-dir is the foot-gun that committed one before).
/plat_*/
# The same rule, for the other trees a run leaves behind: /out is the spike
# scratch (meshy_spike's .glb/.png), /canon_output is a generated pack, and
# /exit is what a stray `> exit` redirect writes. All four were committed once.
/out/
/canon_output/
/exit
.playwright-mcp/

# OS
.DS_Store
Thumbs.db

